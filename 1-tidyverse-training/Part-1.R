#Process of data import to visualization
  #Using packages of 'dplyr' 'ggplot2'. Which are parts of 'tidyverse'

## 1. Installing all packages
  ## since 'install.package("")' doe not work, the packages were installed manually. 
  ## 'http://cran.rstudio.com/bin/windows/contrib/4.2/' or 'https://cran.rstudio.com/src/contrib/'
    ## Then, download the required packages "fs", "readxl","writexl","tidyverse","lubridate", "devtools"
    ## Then installed using 'Tools > Install Packages'


## 1.1 Installing packages SOLVED
  ## use "dependencies = TRUE". Example: install.packages("tidyverse", dependencies = TRUE)

